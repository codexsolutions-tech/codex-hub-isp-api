/* import { messaging } from "../../infrastructure/firebase/firebase";

export default class FirebaseService {

    async enviar(token: string, titulo: string, mensagem: string) {
        await messaging.send({
            token: token,
            notification: {
                title: "Testando",
                body: "Notificação."
            }
        })
    }

    async enviarTodos(tokens: string[]) {

    }

} */